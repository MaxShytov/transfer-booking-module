name: transfer_booking
description: "8Move Transfer - Airport Transfer Booking"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Networking
  dio: ^5.7.0

  # Code Generation
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Routing
  go_router: ^14.6.2

  # Utils
  equatable: ^2.0.7
  url_launcher: ^6.3.1

  # Secure storage for tokens
  flutter_secure_storage: ^9.2.2

  # Local storage
  shared_preferences: ^2.3.3

  # Connectivity
  connectivity_plus: ^6.1.0

  # Google Maps
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  flutter_polyline_points: ^2.1.0

  # SVG support
  flutter_svg: ^2.0.10+1

  # Platform-adaptive widgets
  flutter_platform_widgets: ^7.0.1
  http: ^1.6.0
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Code Generation
  build_runner: ^2.4.14
  freezed: ^2.5.7
  json_serializable: ^6.9.0
  riverpod_generator: ^2.6.3

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/images/
